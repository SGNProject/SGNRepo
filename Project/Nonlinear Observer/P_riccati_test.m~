%% System Matrices

A = zeros(10);

 A(1:3,5:7) = eye(3);
 A(5:7,8:10) = eye(3);
 
 B_ = zeros(10,3);

 B_(8:10,1:3) = eye(3);
   
 row = size(C_dx,1); 

 C1 = zeros(row,6);
  
 


 C = [2*C_dx_sim C1];
 
 %% Tuning Riccati Matrix
 
 R = eye(4);
 Q = 10^(-3)*eye(10);
 
 for i=1:501
     
     C_dx = C_dx_stim(:,:,i),
 
 [P, L, G] = care(A, C', Q, R);